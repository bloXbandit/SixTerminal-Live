flask>=3.0.0
anthropic>=0.25.0
openai>=1.30.0
lxml>=5.0.0

# Deterministic schedule import (offline, no LLM)
openpyxl>=3.1.0          # Excel (.xlsx) extraction
pdfplumber>=0.11.0       # text-layer PDF extraction

# Optional: stronger ruled-table PDF extraction (requires a Java runtime).
# The importer auto-falls back to pdfplumber if this or Java is unavailable.
tabula-py>=2.9.0
